export function setUserId (state, userId) {
  state.userId = userId
}

export function showLoginDialog (state, show) {
  state.showLoginDialog = show
}

export function setIsLoggined (state, login) {
  state.isLoggedIn = login
}
